// Start by creating a variable `message`

const name = "Andriy";
const nameLength = name.length;
const message =
  "My name is " + name + " and it is " + nameLength + " characters long.";
console.log(message);
